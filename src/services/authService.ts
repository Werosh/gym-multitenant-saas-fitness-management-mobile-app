export {
  mockLogin as loginUser,
  mockLogout as logoutUser,
  mockRegister as registerUser,
  mockFetchProfile as fetchUserProfile,
  mockUpdateProfile as updateUserProfile,
} from '../data/mockApi';
